# UTF-8
#
# Windows version resource for the ListenTrace executable (PyInstaller
# `--version-file`). Keep `filevers`/`prodvers`/`FileVersion`/`ProductVersion`
# in sync with `pyproject.toml`'s `[project].version` (four-part
# major.minor.patch.0 -- Windows version resources require exactly four
# integers). This is a manually-maintained file for the Phase A spike; there
# is no build-time generator yet.
#
# CompanyName is deliberately just "ListenTrace" (no separate publisher/
# company name) -- see ROADMAP.md's Phase A decisions.

VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(0, 1, 0, 0),
    prodvers=(0, 1, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          u'040904B0',
          [
            StringStruct(u'CompanyName', u'ListenTrace'),
            StringStruct(u'FileDescription', u'ListenTrace'),
            StringStruct(u'FileVersion', u'0.1.0.0'),
            StringStruct(u'InternalName', u'ListenTrace'),
            StringStruct(u'OriginalFilename', u'ListenTrace.exe'),
            StringStruct(u'ProductName', u'ListenTrace'),
            StringStruct(u'ProductVersion', u'0.1.0.0'),
          ],
        )
      ]
    ),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])]),
  ],
)
